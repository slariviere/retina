{{- if and (or .Values.hubble.enabled .Values.hubble.relay.standalone.enabled) .Values.hubble.relay.enabled .Values.hubble.relay.httpRoute.enabled }}
{{- $baseUrl  := .Values.hubble.relay.httpRoute.baseUrl -}}
{{- $port := .Values.hubble.relay.httpRoute.port | default .Values.hubble.relay.listenPort | default 444 -}}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hubble-relay
  namespace: {{ .Release.Namespace }}
  labels:
    k8s-app: hubble-relay
    app.kubernetes.io/name: hubble-relay
    app.kubernetes.io/part-of: cilium
  {{- with .Values.hubble.relay.httpRoute.labels }}
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- if or .Values.hubble.relay.httpRoute.annotations .Values.hubble.relay.annotations }}
  annotations:
    {{- with .Values.hubble.relay.annotations }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- with .Values.hubble.relay.httpRoute.annotations }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- end }}
spec:
  {{- if .Values.hubble.relay.httpRoute.parentRefs }}
  parentRefs:
    {{- toYaml .Values.hubble.relay.httpRoute.parentRefs | nindent 4 }}
  {{- end }}
  {{- if .Values.hubble.relay.httpRoute.hostnames }}
  hostnames:
    {{- range .Values.hubble.relay.httpRoute.hostnames }}
    - {{ . }}
    {{- end }}
  {{- end }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: {{ $baseUrl | quote }}
      backendRefs:
        - name: hubble-relay
          port: {{ $port }}
{{- end }}
